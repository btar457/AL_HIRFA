/// تصنيف تراثي يُستخدم لتصفية المنتجات في السوق.
class ProductCategory {
  final String name;
  final String icon;
  final int color;

  const ProductCategory({
    required this.name,
    required this.icon,
    required this.color,
  });
}
