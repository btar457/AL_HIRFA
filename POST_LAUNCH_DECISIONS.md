# قرارات مؤجَّلة لما بعد الإطلاق

سجلّ تسجيل فقط — كل بند هنا "مقبول عند الحجم الحالي، مؤجَّل لا محسوم"،
بلا خطة إصلاح ثابتة أو موعد. لا تُصلح أي بند هنا دون تعليمات صريحة جديدة.

## 1. checkout: rate_limits والإشعارات في createOrders تُنفَّذ بعد الدفعة لا داخلها

`lib/services/order_service.dart` — `createOrders()`: الدفعة (`WriteBatch`)
تغطي فقط إنشاء وثائق الطلبات نفسها (ذرّية بالكامل — كلها معاً أو لا شيء).
زيادة عدّاد `rate_limits/{buyerUid}` وإرسال إشعارات "طلب جديد" للحرفي
تُنفَّذان بعد نجاح الدفعة، كخطوتين منفصلتين غير ذرّيتين معها (لا يمكن دمجهما
ضمن الدفعة نفسها دون تعديل `firestore.rules` — راجع تعليق الدالة للتفصيل
التقني).

انقطاع فعلي (تعطّل شبكة، إغلاق التطبيق) بعد نجاح الدفعة مباشرة يترك: عدّاد
`rate_limits` ناقص الزيادة (يُعيد فتح هامش طلبات إضافية للمشتري لاحقاً —
تساهل، لا خطر أمني)، أو إشعاراً لم يصل للحرفي (الطلب موجود فعلاً ومرئي له
عند فتح التطبيق، فقط بلا تنبيه فوري). كلاهما تدهور تدريجي غير حرج عند الحجم
الحالي.

## 2. الفحص المسبق لعدّاد rate_limits خارج الدفعة — نافذة سباق

نفس الدالة: قراءة العدّاد الحالي تحدث قبل بناء الدفعة (وليس ضمن transaction
ذرّية معها) لتقدير إن كانت الدفعة ستتجاوز الحد. مشترٍ بجلستين متزامنتين
(جهازان، أو تبويبان) قد يتجاوز الحد الفعلي قليلاً لأن كل جلسة تقرأ نفس القيمة
قبل أن تكتب الأخرى. الحاجز الأخير الفعلي يبقى قاعدة `orders/create` نفسها
(`firestore.rules`) التي تتحقق من `hourlyCount` وقت كتابة كل طلب فردية —
لكنها تتحقق من `< 10` لكل طلب على حدة فقط، فلا تمنع تجاوزاً طفيفاً ناتجاً عن
هذا السباق بدقة. مقبول: الحد أصلاً Best-effort موثَّق صراحة في
`firestore.rules` نفسها (لا حماية خادم حقيقية حاسمة بلا Cloud Function).

## 3. رسالة خطأ الرفض العامة للمشتري في checkout

`lib/screens/customer/checkout_screen.dart`: عند رفض إنشاء الطلبات (تجاوز
الحد، أو منتج لم يعد `active`)، الرسالة المعروضة
(`AppError.getFirebaseError(e)`) عامة ولا تحدد السبب الفعلي (لا تقول مثلاً
"أحد المنتجات لم يعد متاحاً" أو "لقد تجاوزت عدد الطلبات المسموح"). تحسين
رسائل الخطأ هنا مؤجَّل.
